type Query {
  suggestAddresses(
    searchTerm: String!
    sessionToken: String
  ): [AddressSuggestion!]!

  getAddressByExternalId(id: String!, sessionToken: String): Address!

  providerLogo: Image!

  sessionToken: String!
}
