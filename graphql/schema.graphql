type Query {
  addressSuggestions(
    searchTerm: String!
    sessionToken: String
  ): [AddressSuggestion!]!

  address(externalId: String!, sessionToken: String): Address!

  providerLogo: Image!

  sessionToken: String!
}
